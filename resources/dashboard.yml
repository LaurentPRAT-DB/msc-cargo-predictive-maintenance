resources:
  dashboards:
    predictive_maintenance_dashboard:
      display_name: "[${bundle.target}] MSC Cargo — Predictive Maintenance"
      file_path: ../src/dashboards/predictive_maintenance.lvdash.json
      warehouse_id: ${var.warehouse_id}
      permissions:
        - level: CAN_RUN
          group_name: "users"
